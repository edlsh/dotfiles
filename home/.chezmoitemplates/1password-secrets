{{- /* 1Password secrets - only if configured in chezmoidata */ -}}
{{- if and (stat (joinPath .chezmoi.homeDir ".config/op/config")) (hasKey . "secrets") -}}
# API Keys from 1Password (injected at chezmoi apply time)
{{- range $name, $ref := .secrets }}
export {{ $name }}='{{ onepasswordRead $ref }}'
{{- end }}
{{- end }}
